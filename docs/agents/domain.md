# Domain Docs

Single-context repo:

```
/
├── CONTEXT.md
├── docs/adr/
│   ├── 0001-*.md
│   └── ...
└── app/
```

## Before exploring, read these

- **`CONTEXT.md`** at the repo root for domain vocabulary
- **`docs/adr/`** for architectural decisions in the area you're about to work in

If these files don't exist, proceed silently. They're created lazily when terms or decisions crystallize.
